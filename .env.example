NEXT_PUBLIC_SITE_URL=http://localhost:3000
PAYMENT_PROVIDER=demo
PAYMENT_WEBHOOK_SECRET=replace-with-a-long-random-secret
# Production only: configure the chosen processor's private API keys in the hosting secret manager.
# Never put card data or real secrets in this file.

