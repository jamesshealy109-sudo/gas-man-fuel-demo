import { membershipPlans } from "@/src/config/business";import { faqs } from "@/src/lib/demo-data";import { PageHero,PublicPage } from "../components/SiteChrome";
export default function Membership(){return <PublicPage><PageHero eyebrow="THE GAS MAN MEMBERSHIP" title="Always moving. Never running low." intro="A simple monthly membership for mobile fuel delivery — with immediate activation after confirmed payment." actions={<a className="button primary" href="/join">Join now →</a>}/><section className="plans-page">{membershipPlans.map(p=><article className={p.featured?"featured":""} key={p.id}>{p.featured&&<span className="plan-flag">MOST POPULAR</span>}<p className="eyebrow">MONTHLY MEMBERSHIP</p><h2>{p.name}</h2><strong>${p.monthlyPrice}<small>/month</small></strong><p>{p.description}</p><ul><li>Covers {p.vehicles} vehicle{p.vehicles>1?"s":""}</li><li>Up to {p.fillUps} fill-ups per month</li><li>$0.10 off per gallon</li><li>Optional $10 gas-can top-off add-on</li></ul><a className="button primary" href={`/join?plan=${p.id}`}>Choose {p.name}</a></article>)}</section><section className="activation-explainer"><p className="eyebrow">WHAT CHANGES</p><h2>Customer joins. Payment confirms.<br/><em>Membership activates. Done.</em></h2><div className="flow-row">{["Complete signup","Payment authorization","Secure confirmation","Membership active","Staff sees member"].map((s,i)=><div key={s}><span>{i+1}</span><strong>{s}</strong>{i<4&&<b>→</b>}</div>)}</div><p>Your staff sees the new member immediately after payment confirmation — not the next morning.</p></section><section className="faq-preview membership-faq"><div><p className="eyebrow">MEMBERSHIP FAQ</p><h2>Clear billing.<br/>Clear status.</h2></div><div>{faqs.slice(4).map(([q,a])=><details key={q}><summary>{q}<span>+</span></summary><p>{a}</p></details>)}</div></section></PublicPage>}

