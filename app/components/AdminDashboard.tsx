"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import { useMemo,useState } from "react";
import { demoMembers,serviceRequests,MemberStatus } from "@/src/lib/demo-data";
const label=(s:MemberStatus)=>s.replace("_"," ");
export function AdminDashboard(){const [query,setQuery]=useState("");const [filter,setFilter]=useState<"all"|MemberStatus>("all");const members=useMemo(()=>demoMembers.filter(m=>(filter==="all"||m.status===filter)&&`${m.name} ${m.memberNumber} ${m.email}`.toLowerCase().includes(query.toLowerCase())),[query,filter]);return <div className="admin-layout"><aside className="admin-side"><a className="brand" href="/"><span className="brand-mark">GM</span><span><strong>THE GAS MAN</strong><small>OPERATIONS</small></span></a><nav><a className="active" href="#overview">Overview</a><a href="#members">Members <b>25</b></a><a href="#payments">Payments</a><a href="#requests">Service requests <b>4</b></a><a href="/demo/current-vs-new">Workflow demo</a></nav><div className="admin-user"><span>OP</span><div><strong>Operations Demo</strong><small>admin@demo.example</small></div></div></aside><main className="admin-main"><header><div><p>THURSDAY, AUGUST 27</p><h1>Good morning, Operations</h1></div><a className="button primary" href="/join">+ Add member</a></header><section id="overview"><div className="metric-grid"><article><span>ACTIVE MEMBERS</span><strong>20</strong><small>80% of all memberships</small></article><article><span>NEW TODAY</span><strong>3</strong><small className="up">↑ Live after payment</small></article><article><span>PENDING PAYMENTS</span><strong>2</strong><small>Needs attention</small></article><article><span>MONTHLY RECURRING REVENUE</span><strong>$680</strong><small>Membership fees</small></article></div><div className="ops-grid"><article className="panel"><div className="panel-head"><div><span>LIVE ACTIVITY</span><h2>Recent signups</h2></div><span className="live-dot">LIVE</span></div>{demoMembers.slice(0,4).map((m,i)=><div className="signup-row" key={m.id}><span className="avatar">{m.name.split(" ").map(n=>n[0]).join("")}</span><div><a href={`/admin/members/${m.id}`}>{m.name}</a><small>{m.plan} · {m.memberNumber}</small></div><strong>{i===0?"2 min":`${i*7+5} min`} ago</strong></div>)}</article><article className="panel activation-panel"><span className="live-dot">CORE IMPROVEMENT</span><h2>Membership activation</h2><div><strong>Near-real-time</strong><span>after confirmed payment</span></div><p>Customer joins. Payment confirms. Membership activates. Done.</p><a href="/demo/current-vs-new">See current vs. proposed →</a></article></div></section><section id="members" className="panel members-panel"><div className="panel-head"><div><span>CRM-LITE</span><h2>Members</h2></div><div className="table-tools"><input aria-label="Search members" placeholder="Search name, email, member #" value={query} onChange={e=>setQuery(e.target.value)}/><select aria-label="Filter status" value={filter} onChange={e=>setFilter(e.target.value as typeof filter)}><option value="all">All statuses</option><option value="active">Active</option><option value="pending_payment">Pending</option><option value="past_due">Past due</option><option value="canceled">Canceled</option></select></div></div><div className="table-wrap"><table><thead><tr><th>Member</th><th>Contact</th><th>Plan</th><th>Status</th><th>Joined</th><th>Last payment</th></tr></thead><tbody>{members.map(m=><tr key={m.id}><td><a href={`/admin/members/${m.id}`}><b>{m.name}</b><small>{m.memberNumber}</small></a></td><td>{m.phone}<small>{m.email}</small></td><td>{m.plan}</td><td><span className={`status ${m.status}`}>{label(m.status)}</span></td><td>{m.joined}<small>{m.joinedTime}</small></td><td>{m.lastPayment}</td></tr>)}</tbody></table></div></section><section id="requests" className="panel members-panel"><div className="panel-head"><div><span>WORK QUEUE</span><h2>Service requests</h2></div><a href="/service-request">Open request form →</a></div><div className="table-wrap"><table><thead><tr><th>Request</th><th>Customer</th><th>Type</th><th>Preferred</th><th>Status</th></tr></thead><tbody>{serviceRequests.map(r=><tr key={r.id}><td><b>{r.id}</b></td><td>{r.customer}</td><td>{r.type}</td><td>{r.preferred}</td><td><span className="status active">{r.status}</span></td></tr>)}</tbody></table></div></section></main></div>}
