import { business } from "@/src/config/business";import { PageHero,PublicPage } from "../components/SiteChrome";
export default function Area(){return <PublicPage><PageHero eyebrow="GREATER COLUMBIA AREA" title="Your neighborhood is our route." intro="Published coverage includes Columbia, Blythewood, Elgin, Lugoff, Camden, and nearby areas." actions={<a className="button primary" href={`tel:${business.phoneHref}`}>Check by phone</a>}/><section className="area-page"><div className="map-graphic large" aria-hidden="true"><span className="map-ring one"></span><span className="map-ring two"></span><span className="map-pin main">COLUMBIA</span><span className="map-pin north">BLYTHEWOOD</span><span className="map-pin east">LUGOFF · CAMDEN</span><span className="map-pin mid">ELGIN</span></div><div><p className="eyebrow">CURRENTLY SERVING</p><ul>{business.serviceAreas.map(a=><li key={a}>{a}<span>SC</span></li>)}</ul><p>Just outside the listed area? Routes and availability can vary, so call before booking.</p><a href="/service-request">Request service →</a></div></section></PublicPage>}

